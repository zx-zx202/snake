# 基于 MediaPipe + OpenCV 的手势识别与视觉交互小项目

这是一个基于 **Python、OpenCV、cvzone、MediaPipe** 实现的手势交互小项目，当前包含两个演示功能：

1. **手势控制贪吃蛇游戏**  
   通过摄像头识别手部关键点，使用食指指尖位置控制蛇头移动，完成“隔空玩贪吃蛇”的交互效果。

2. **中文版手势识别演示**  
   通过摄像头实时检测手部，并对常见手势进行识别，同时在画面中以**中文**方式显示识别结果。

这个项目比较适合作为：
- 计算机视觉入门练习
- 人机交互课程小作业
- MediaPipe / OpenCV 学习案例
- 后续做更复杂手势识别、手语识别系统的基础Demo

---

# 项目功能

## 1. 手势控制贪吃蛇
`basic.py` 实现了一个简单的视觉交互小游戏：
- 调用电脑摄像头采集实时画面
- 检测单只手
- 提取食指指尖坐标
- 将食指移动轨迹映射为蛇头运动轨迹
- 当蛇头触碰到食物后自动加分、增长长度
- 游戏结束后显示得分

这个功能的重点不是传统键盘控制，而是**用手势轨迹代替鼠标/方向键**，实现更直观的交互方式。

## 2. 中文手势识别演示
`hand.py` 实现了一个带中文显示的手势识别程序，主要功能包括：
- 检测最多两只手
- 绘制手部关键点和骨架连线
- 根据关键点位置关系判断手势类别
- 在图像中显示中文识别结果
- 支持中英文控制台提示信息

当前已经实现的手势类别包括：
- 张开手掌
- 握拳
- 食指指向
- 胜利手势
- OK 手势
- 点赞
- 点踩
- 打电话
- 未知手势

---

# 项目结构

```bash
.
├── basic(2).py          # 手势控制贪吃蛇
├── hand(1).py           # 中文手势识别演示
├── requirements(1).txt  # 项目依赖
├── donut.png            # 贪吃蛇食物图片资源
└── README.md            # 项目说明文档
```

> 注意：当前代码中 `basic.py` 默认读取的图片名是 `donut.png`。  
> 如果你的资源文件名不是这个名字，比如是 `donut.png`，需要**手动改名为 `donut.png`**，或者修改代码中的图片路径。

---

# 运行环境

## 开发环境建议
- Python 3.8 ~ 3.10
- Windows 10 / Windows 11
- 电脑自带或外接 USB 摄像头

## 主要依赖库
从代码本身看，核心依赖主要有：
- `opencv-python`
- `cvzone`
- `mediapipe`
- `numpy`
- `Pillow`

项目里还提供了完整的 `requirements.txt`，可以直接统一安装。fileciteturn0file2

---

# 安装方法

## 1. 克隆或下载项目
将项目文件下载到本地后，确保以下文件在同一目录下：
- `basic(2).py`
- `hand(1).py`
- `requirements(1).txt`
- `donut.png`

## 2. 安装依赖
在项目目录打开终端，执行：

```bash
pip install -r requirements(1).txt
```

如果你只想先运行当前两个 Demo，也可以按最小依赖安装：

```bash
pip install opencv-python numpy pillow mediapipe cvzone
```

---

# 使用方法

## 一、运行手势控制贪吃蛇
执行：

```bash
python basic.py
```

程序运行后：
- 摄像头会自动打开
- 识别你的手部
- 使用**食指指尖**控制蛇头移动
- 吃到甜甜圈图片后会加分并增长
- 按键盘 **R** 可在结束后重新开始

### 对应代码说明
该脚本主要流程如下：
- 调用摄像头读取画面
- 使用 `HandDetector` 检测手部
- 获取食指指尖关键点 `lmList[8]`
- 将指尖坐标作为蛇头位置
- 根据蛇身长度、碰撞情况更新游戏状态

这些逻辑都已经在源码中实现。fileciteturn0file0

---

## 二、运行中文手势识别程序
执行：

```bash
python hand.py
```

程序运行后：
- 摄像头会自动打开
- 实时检测手部关键点
- 在画面中绘制手部骨架
- 自动识别当前手势
- 在图像中显示中文结果
- 按键盘 **Q** 退出程序

### 对应代码说明
该脚本主要包含两个核心类：

#### 1. `ChineseTextRenderer`
用于解决 OpenCV 默认不方便显示中文的问题，内部通过 `PIL.ImageDraw` 和字体文件将中文绘制到图像上。源码中已经写了多组字体路径，优先适配 Windows，也兼容部分 Mac 和 Linux 路径。fileciteturn0file1

#### 2. `GestureRecognizer`
用于：
- 检测手部
- 计算关键点之间距离
- 根据手指伸展状态判断手势类型
- 绘制边界框、骨架连线、中文识别信息

当前手势识别主要基于规则判断，也就是根据各手指关键点间距离和伸展状态来区分不同手势。fileciteturn0file1

---

# 支持的手势类别

根据当前源码中的手势映射表，程序支持以下中文手势名称：

| 英文标识 | 中文名称 |
|---|---|
| open_hand | 张开手掌 |
| fist | 握拳 |
| pointing | 食指指向 |
| victory | 胜利手势 |
| ok | OK手势 |
| thumbs_up | 点赞 |
| thumbs_down | 点踩 |
| call_me | 打电话 |
| unknown | 未知手势 |

这些类别直接定义在 `hand.py` 的 `self.gestures` 字典中。fileciteturn0file1

---

# 代码特点

## 1. 交互性比较强
不是单纯地做图像显示，而是加入了“手势控制游戏”和“实时手势识别反馈”，上手会更直观。

## 2. 中文显示友好
很多 OpenCV 示例只能显示英文，这个项目额外封装了中文渲染类，能更方便地展示中文识别结果。fileciteturn0file1

## 3. 适合继续扩展
这个项目虽然是 Demo，但很适合继续往下面几个方向扩展：
- 增加更多静态手势类别
- 改进手势识别逻辑，减少误判
- 加入动态手势识别
- 与手语识别系统结合
- 与小游戏、智慧交互、课堂展示项目结合

---

# 注意事项

## 1. 摄像头权限
如果程序运行后打不开摄像头，先检查：
- 电脑是否有可用摄像头
- 是否被其他软件占用
- 系统是否已授予 Python 摄像头权限

## 2. 图片资源路径
贪吃蛇脚本默认读取：

```python
game = SnakeGameClass("donut.png")
```

所以一定要保证图片资源存在，且文件名一致。fileciteturn0file0

## 3. 中文字体问题
如果中文显示乱码，通常是因为系统中没有找到可用中文字体。当前代码会优先尝试以下字体路径：
- `C:/Windows/Fonts/simhei.ttf`
- `C:/Windows/Fonts/simsun.ttc`
- `/System/Library/Fonts/PingFang.ttc`
- `/usr/share/fonts/truetype/droid/DroidSansFallbackFull.ttf`

如果都加载失败，程序会退回默认字体，可能导致中文显示效果不理想。fileciteturn0file1

## 4. 依赖版本问题
`requirements.txt` 中部分库版本比较老，尤其是 `mediapipe`、`opencv-python`、`tensorflow` 等，如果你本地 Python 版本较新，可能出现安装冲突。遇到这种情况，可以：
- 优先新建虚拟环境
- 先安装最小依赖运行 Demo
- 再根据实际需求补充其他库

这个建议主要是因为当前两个 Demo 实际用到的库并没有 `requirements.txt` 里列得那么多。依赖文件内容可直接查看。fileciteturn0file2

---

# 后续可优化方向

如果后面继续完善这个项目，可以考虑做下面这些升级：

## 1. 提高手势识别准确率
当前识别方式是规则法，优点是简单直接，但复杂场景下容易误判。后续可以考虑：
- 加入更稳定的手指角度判断
- 使用时序信息平滑结果
- 引入分类模型替代纯规则判断

## 2. 增加动态手势功能
目前更偏向静态手势识别。后面可以加入：
- 挥手
- 画圈
- 左右滑动
- 数字手势轨迹

## 3. 提升游戏可玩性
贪吃蛇部分可以继续增加：
- 碰撞检测
- 边界限制
- 多种食物
- 难度等级
- 开始界面和结束界面优化

## 4. 面向手语识别扩展
如果以后要往手语识别方向做，这个项目可以作为前期基础：
- 当前版本适合做手部关键点采集与交互验证
- 后续可以替换成序列建模方法
- 再进一步做连续手语识别、句子理解和交互展示

---

# 总结

这个项目虽然规模不大，但很适合作为一个 **“计算机视觉 + 手势交互” 的入门练手项目**。  
它一方面展示了如何使用 MediaPipe 做手部关键点检测，另一方面也把识别结果和实际交互结合起来了，不是只停留在“识别出来”这一步。

如果你是刚接触 OpenCV、MediaPipe 或手势识别，这个项目很适合拿来学习；如果你后面想继续做更复杂的视觉交互、手语识别或者智能演示系统，它也可以作为一个不错的起点。基于现有两个脚本的功能与结构，我整理了上面的 README 内容。fileciteturn0file0 fileciteturn0file1
